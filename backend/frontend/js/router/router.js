/** * Code Spark - Client-Side Single Page Application Router * Handles hash-based routing, role protection, and view lifecycle management. */ import AuthService from '../auth/authService.js'; import { Toast } from '../components/ui.js'; import { StudentPages } from '../pages/studentPages.js'; import { AdminPages } from '../pages/adminPages.js'; export class Router { static routes = {}; static currentCleanup = null; static register(pattern, handler, requiredRoles = null) { this.routes[pattern] = { handler, requiredRoles }; } static init() { window.addEventListener('hashchange', () => this.handleRoute()); window.addEventListener('codespark:auth-expired', () => { window.location.hash = '#/login'; }); this.handleRoute(); } static async handleRoute() { const hash = window.location.hash || '#/'; const path = hash.replace(/^#/, '') || '/'; const mainContent = document.getElementById('content-container') || document.getElementById('main-content'); const appShell = document.getElementById('app-shell'); const authShell = document.getElementById('auth-shell'); // Auth-only vs Public Pages const isPublic = path === '/login' || path === '/register'; const isAuthed = AuthService.isAuthenticated(); // Toggle navigation UI visibility based on auth state IMMEDIATELY if (isAuthed) { if (authShell) authShell.style.display = 'none'; if (appShell) appShell.style.display = 'flex'; if (isPublic || path === '/') { const user = AuthService.getUser(); window.location.hash = (user?.role === 'admin' || user?.role === 'assistant') ? '#/admin/dashboard' : '#/student/dashboard'; return; } } else { if (appShell) appShell.style.display = 'none'; if (authShell) authShell.style.display = 'flex'; if (!isPublic && path !== '/') { window.location.hash = '#/login'; return; } } // Match route let matchedRoute = null; let params = {}; for (const [pattern, config] of Object.entries(this.routes)) { const paramNames = []; const regexPath = pattern.replace(/:([a-zA-Z0-9_]+)/g, (_, key) => { paramNames.push(key); return '([^/]+)'; }); const match = path.match(new RegExp(`^${regexPath}$`)); if (match) { matchedRoute = config; paramNames.forEach((name, idx) => { params[name] = match[idx + 1]; }); break; } } if (!matchedRoute) { if (mainContent) { mainContent.innerHTML = ` 

## 404 - الصفحة غير موجودة

الصفحة التي تحاول الوصول إليها غير متاحة.

[العودة للرئيسية](#/)

`; } return; } // Role Guard Check if (matchedRoute.requiredRoles) { if (!AuthService.hasRole(matchedRoute.requiredRoles)) { Toast.error('ليس لديك الصلاحية لدخول هذه الصفحة'); window.location.hash = '#/dashboard'; return; } } // Update active state in sidebar document.querySelectorAll('.nav-item').forEach(item => { const itemHash = item.getAttribute('href'); if (itemHash && hash.startsWith(itemHash)) { item.classList.add('active'); } else { item.classList.remove('active'); } }); // Execute route handler if (this.currentCleanup) { try { this.currentCleanup(); } catch (_) {} this.currentCleanup = null; } if (mainContent) { mainContent.innerHTML = ''; window.scrollTo({ top: 0, behavior: 'instant' }); const cleanup = await matchedRoute.handler(mainContent, params); if (typeof cleanup === 'function') { this.currentCleanup = cleanup; } } } }