[project]
name = "codespark"
version = "1.0.0"
requires-python = ">=3.11,<3.15"
dependencies = [
    "fastapi[standard]",
]

[tool.fastapi]
entrypoint = "app.main:app"

[tool.setuptools]
packages = ["app"]